[project]
name = "production-grade-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-openrouter>=0.2.8",
]

[project.scripts]
production-grade-rag = "main:main"
